#include <stdio.h>
#include <iostream>
#include "Debounce.h"

using namespace std;

int main()
{
    // DebounceEvent *A = new DebounceEvent();
    // DebounceEvent *B = new BFunctionEvent();
    // ABFunctionEvent *AB = new ABFunctionEvent();

    // // A->Execute();

    // delete AB;
    // delete B;
    // delete A;
    return 0;
}